const soldoutButton = () => {
    // console.log(soldout);
    tvGenerator(soldout);
};

const wholeListButton = () => {
    // console.log(inventory);
    tvGenerator(inventory);
};

const ambiButton = () => {
    // console.log(ambilight);
    tvGenerator(ambilight);
};
